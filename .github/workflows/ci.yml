name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 2 * * *"   # 02:00 UTC daily
  workflow_dispatch:
  repository_dispatch:
    types: [wasmagent-js-updated]

permissions:
  contents: read

jobs:
  ci:
    name: Typecheck, Test, Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout bscode
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6
        with:
          bun-version: "1.3.14"

      # @wasmagent/* 1.0.0 published to npm — no sibling checkout needed.
      - name: Install dependencies
        run: bun install --frozen-lockfile

      # BS-2 / BS-5: a semver range is not evidence of what was tested. Record
      # the exact frozen lock hash and the resolved @wasmagent versions that
      # this job actually ran against.
      - name: Record locked dependency resolution (BS-2/BS-5)
        run: |
          set -euo pipefail
          {
            echo "### Locked dependency resolution (BS-2/BS-5)"
            echo ""
            echo '```'
            echo "bun.lock sha256: $(sha256sum bun.lock | cut -d' ' -f1)"
            (cd apps/worker && bun pm ls) | grep -E "@wasmagent/[a-z-]+@" || true
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Branding check
        run: node scripts/check-branding.mjs

      - name: Typecheck (worker)
        working-directory: apps/worker
        run: bunx tsc --noEmit

      - name: Typecheck (web)
        working-directory: apps/web
        run: bunx tsc --noEmit

      - name: Test (worker)
        working-directory: apps/worker
        run: bun run test

      - name: Test (web)
        working-directory: apps/web
        run: bun run test

      - name: Build (web)
        working-directory: apps/web
        run: bun run build
        env:
          NEXT_TELEMETRY_DISABLED: 1

      # A1 — Cloudflare Worker bundle size budget (6 MB uncompressed).
      # Guards against pyodide / browser tools / model SDKs being statically bundled.
      - name: Worker bundle size budget (A1)
        working-directory: apps/worker
        run: |
          bun run build
          node ../../scripts/check-bundle-size.mjs

      # A2 — Validate fixture rollouts conform to rollout-wire/v1 schema.
      # Ensures the shared fixture byte-matches the contract and that all
      # required fields are present with correct types.
      - name: Rollout fixture schema validation (A2)
        run: node scripts/check-rollout-schema.mjs

      # A3 — Production config lint: warn if BSCODE_ALLOWED_ORIGIN is still
      # the local dev default. Does not block CI (deploy is gated on secrets),
      # but surfaces misconfiguration early.
      - name: Production config lint (A3)
        run: |
          ORIGIN=$(grep -r "BSCODE_ALLOWED_ORIGIN" apps/worker/wrangler.toml | grep -v "^#" | grep "localhost" || true)
          if [ -n "$ORIGIN" ]; then
            echo "::warning::BSCODE_ALLOWED_ORIGIN is still set to localhost in wrangler.toml — update before deploying to production."
          else
            echo "✓ BSCODE_ALLOWED_ORIGIN does not contain localhost"
          fi

  # CI green is NOT deploy green. Deployment lives in deploy.yml with
  # environment: production and explicit configured/not-configured semantics.

  # BS-5 locked lane: verify bscode installs cleanly from published npm
  # packages (frozen lock) and that the worker tests pass against that exact
  # resolved set. This is the certified/locked counterpart to canary.yml.
  locked-published:
    name: Locked published packages
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' || github.event_name == 'repository_dispatch'

    steps:
      - name: Checkout bscode
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6
        with:
          bun-version: "1.3.14"

      - name: Install from npm only (no local wasmagent-js)
        run: bun install --frozen-lockfile

      - name: Typecheck (worker)
        working-directory: apps/worker
        run: bunx tsc --noEmit

      - name: Test (worker)
        working-directory: apps/worker
        run: bun run test

      - name: Test (web)
        working-directory: apps/web
        run: bun run test
